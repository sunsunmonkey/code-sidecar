/**
 * Type definitions index
 * Central export point for all type definitions used in the configuration system
 */

// Export configuration-related types
export type {
  UIConfiguration,
  ExportedConfiguration,
  ValidationErrors
} from './config';

// Export message-related types
export type {
  ApiConfiguration,
  ConfigMessage,
  ConfigResponse
} from './messages';
